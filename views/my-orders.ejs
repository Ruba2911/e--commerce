<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Orders - Elegant Store</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav>
    <div class="nav-container">
      <a href="/" class="logo">✨ Elegant Store</a>
      <ul class="nav-links">
        <li><a href="/">Home</a></li>
        <li><a href="/products">Products</a></li>
        <li><a href="/orders/my-orders">My Orders</a></li>
        <li><a href="/auth/logout">Logout (<%= user.username %>)</a></li>
        <li>
          <a href="/cart" class="cart-icon">
            🛒
            <% if (cart && cart.length > 0) { %>
              <span class="cart-count"><%= cart.length %></span>
            <% } %>
          </a>
        </li>
      </ul>
    </div>
  </nav>

  <div class="container">
    <h1 style="font-size: 2.5rem; margin-bottom: 2rem; color: var(--primary);">My Orders</h1>

    <% if (orders.length === 0) { %>
      <div class="empty-state">
        <h2>No orders yet</h2>
        <p>Start shopping to see your orders here!</p>
        <a href="/products" class="btn btn-primary">Browse Products</a>
      </div>
    <% } else { %>
      <div style="display: grid; gap: 1.5rem;">
        <% orders.forEach(order => { %>
          <div style="background: white; padding: 2rem; border-radius: 15px; box-shadow: var(--shadow);" class="fade-in">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
              <div>
                <h3 style="color: var(--primary);">Order #<%= order.id %></h3>
                <p style="color: #7f8c8d;"><%= new Date(order.created_at).toLocaleDateString() %></p>
              </div>
              <div style="text-align: right;">
                <div style="font-size: 1.5rem; font-weight: bold; color: var(--secondary);">
                  $<%= order.total.toFixed(2) %>
                </div>
                <span style="background: var(--success); color: white; padding: 0.3rem 1rem; border-radius: 20px; font-size: 0.9rem;">
                  <%= order.status %>
                </span>
              </div>
            </div>
            <p style="color: #7f8c8d; margin-bottom: 1rem;">
              <strong>Shipping to:</strong> <%= order.shipping_address %>
            </p>
            <a href="/orders/confirmation/<%= order.id %>" class="btn btn-primary">View Details</a>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>

  <footer>
    <p>&copy; 2024 Elegant Store. All rights reserved. | Premium Quality Products</p>
  </footer>
</body>
</html>
